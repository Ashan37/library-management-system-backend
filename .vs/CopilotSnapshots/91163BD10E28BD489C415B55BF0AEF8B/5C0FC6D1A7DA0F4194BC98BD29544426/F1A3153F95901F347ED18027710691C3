using library_management_system_backend.Data;
using library_management_system_backend.Models;
using Microsoft.EntityFrameworkCore;
using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Mvc;

namespace library_management_system_backend.Controllers
{
    [Route("api/[controller]")]
    [ApiController]
    public class UserController : ControllerBase
    {
        private readonly AppDbContext _appDbContext;

        public UserController(AppDbContext appDbContext)
        {
            _appDbContext = appDbContext;
        }

        [HttpGet]
        [Route("getAllUsers")]
        public async Task<IEnumerable<User>> GetAllUsers()
        {
            return await _appDbContext.Users.ToListAsync();
        }

        [HttpGet]
        [Route("getUser/{id}")]
        public async Task<ActionResult<User>> GetUser(string id)
        {
            var user = await _appDbContext.Users.FindAsync(id);

            if (user == null)
            {
                return NotFound();
            }

            return user;
        }

        [HttpPost]
        [Route("addUser")]
        public async Task<ActionResult<User>> AddUser(User objUser)
        {
            _appDbContext.Users.Add(objUser);
            await _appDbContext.SaveChangesAsync();
            
            return CreatedAtAction(nameof(GetUser), new { id = objUser.id }, objUser);
        }

        [HttpPatch]
        [Route("updateUser/{id}")]
        public async Task<IActionResult> UpdateUser(string id, User objUser)
        {
            if (id != objUser.id)
            {
                return BadRequest("ID mismatch");
            }

            _appDbContext.Entry(objUser).State = EntityState.Modified;

            try
            {
                await _appDbContext.SaveChangesAsync();
            }
            catch (DbUpdateConcurrencyException)
            {
                var exists = await _appDbContext.Users.AnyAsync(u => u.id == id);
                if (!exists)
                {
                    return NotFound();
                }
                throw;
            }

            return NoContent();
        }

        [HttpDelete]
        [Route("deleteUser/{id}")]
        public async Task<IActionResult> DeleteUser(string id)
        {
            var user = await _appDbContext.Users.FindAsync(id);

            if (user == null)
            {
                return NotFound();
            }

            _appDbContext.Users.Remove(user);
            await _appDbContext.SaveChangesAsync();

            return NoContent();
        }
    }
}
